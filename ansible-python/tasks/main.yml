---
# tasks file for ansible_vim
- name: Install python3
  dnf:
    name: python3
    state: latest
  become: yes

# tasks file for ansible_vim
- name: Install pip3
  dnf:
    name: python3-pip
    state: latest
  become: yes
